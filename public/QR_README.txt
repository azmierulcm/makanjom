Save your CIMB DuitNow QR screenshot here as:

  /public/qr-payment.png

The checkout page at /vendor (Upgrade tab) will display it automatically
inside the branded CIMB DuitNow card. The card already shows your name
(Muhammad Azmierul Bin Che Mat) and the CIMB / DuitNow branding.

Steps:
1. Open your CIMB app → DuitNow QR → screenshot the QR
2. Crop to just the QR card (white background, pink border)
3. Save as qr-payment.png in this /public folder
4. The checkout card will show it inside the pink DuitNow frame
